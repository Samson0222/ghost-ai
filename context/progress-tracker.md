# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Feature 27 (Spec Generation Flow) — complete

## Current Goal

- Continue with next feature spec after Feature 27.

## Completed

- Cleaned up Next.js boilerplate (stripped globals.css, removed SVGs, replaced page.tsx with minimal stub).
- **Design system** — shadcn/ui initialized, all 7 UI primitive components added (Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea), lucide-react installed, `lib/utils.ts` with `cn()` in place, `globals.css` rewritten as dark-only with full design system token set.
- **Editor chrome** — `EditorNavbar` (fixed top bar, PanelLeftOpen/Close toggle), `ProjectSidebar` (fixed floating overlay, slide-in from left, My Projects / Shared tabs, New Project button), `EditorDialog` (reusable dialog pattern with title/description/footer slots). Components wired in `app/page.tsx`.
- **Authentication** — Clerk wired in: `ClerkProvider` in root layout with `dark` theme from `@clerk/ui/themes` and CSS variable overrides; `proxy.ts` at project root using `clerkMiddleware` and `createRouteMatcher` to protect all non-auth routes; sign-in and sign-up pages at `/sign-in` and `/sign-up` with two-panel layout (logo + feature list left, Clerk form right; form-only on small screens); `/` redirects to `/editor`; `UserButton` in editor navbar right section. Editor moved to `app/(editor)/editor/page.tsx`; auth pages isolated in `app/(auth)/`.
- **Project dialogs (Feature 04)** — editor home screen with project grid, create/rename/delete dialogs, sidebar project actions with mobile backdrop. Mock data only.
- **Prisma (Feature 05)** — `prisma/models/project.prisma` with `Project` and `ProjectCollaborator` models; `lib/prisma.ts` cached singleton branching on `prisma+postgres://` (Accelerate) vs direct `@prisma/adapter-pg`; migration `20260519061317_init` applied; client generated to `app/generated/prisma`.
- **Project APIs (Feature 06)** — `app/api/projects/route.ts` (GET list, POST create); `app/api/projects/[projectId]/route.ts` (PATCH rename, DELETE). Clerk `auth()` enforces 401 for unauthenticated requests; owner check enforces 403 for non-owner mutations. POST defaults missing name to `'Untitled Project'`. `npm run build` passes.
- **Wire Editor Home (Feature 07)** — `lib/projects.ts` with `getMyProjects()`/`getSharedProjects()` server helpers and `ProjectRecord` interface; `EditorLayout` is now a server component that fetches both lists and passes to `EditorShell`; `EditorShell` accepts project props and uses `useProjectActions` (replacing mock `useProjectDialogs`); `hooks/use-project-actions.ts` calls real API endpoints — POST creates and navigates to `/editor/[id]`, PATCH renames and calls `router.refresh()`, DELETE redirects to `/editor` if deleting active workspace else refreshes; `editor/page.tsx` is a server component using `NewProjectButton` (client component) for the create trigger; `ProjectSidebar` uses `ProjectRecord` type, removed mock data; dialogs updated to use `ProjectRecord`; create dialog shows Room ID preview (slugified name + per-session suffix). `npm run build` passes.
- **Editor Workspace Shell (Feature 08)** — `lib/project-access.ts` with `getCurrentUser()` (userId + primary email via Clerk `currentUser()`) and `getProjectWithAccess(projectId)` (checks owner or collaborator by email); `components/editor/access-denied.tsx` with centered layout, lock icon, message, and link back to `/editor`; `app/(workspace)/editor/[roomId]/page.tsx` server component — unauthenticated users redirect to `/sign-in`, missing or unauthorized projects render `AccessDenied`, valid projects render `WorkspaceShell`; `components/editor/workspace-shell.tsx` client component with full-viewport layout — fixed navbar showing project name + share button + AI sidebar toggle + UserButton, existing `ProjectSidebar` with `activeProjectId` highlight, canvas placeholder, toggleable right AI sidebar placeholder; `ProjectSidebar` updated with optional `activeProjectId` prop (active item styled with `bg-subtle` + `text-copy-primary font-medium`). Workspace lives in `app/(workspace)/` route group (separate from `app/(editor)/`) so it uses root layout only, not `EditorShell`. `npm run build` passes.
- **Share Dialog (Feature 09)** — `getProjectWithAccess` extended to return `ProjectWithRole` (`isOwner: boolean`); `app/api/projects/[projectId]/collaborators/route.ts` (GET list enriched via Clerk `getUserList`, POST invite with owner-only guard, email validation, duplicate check); `app/api/projects/[projectId]/collaborators/[collaboratorId]/route.ts` (DELETE, owner-only); `hooks/use-share-dialog.ts` manages open/close, load, invite, remove, copy-link with optimistic remove; `components/editor/share-dialog.tsx` — project link section with Copy/Copied! toggle, scrollable collaborator list with Avatar+fallback initials, remove button (owner only), invite form with inline error (owner only); `WorkspaceShell` updated to accept `isOwner`, call `useShareDialog`, wire Share button and render `ShareDialog`; workspace page passes `isOwner` destructured from `ProjectWithRole`. shadcn Avatar component added. `npm run build` passes.
- **Liveblocks Setup (Feature 10)** — `@liveblocks/node@3.19.1` installed; `liveblocks.config.ts` updated with `Presence` (cursor `{x,y}|null`, `isThinking: boolean`) and `UserMeta` (id, name, avatar, color); `lib/liveblocks.ts` — lazy `getLiveblocks()` factory (defers instantiation to avoid build-time env errors), cached via `globalThis` in dev, `getUserColor()` deterministically maps user ID to one of 10 fixed cursor colors; `app/api/liveblocks-auth/route.ts` — POST, requires Clerk auth, verifies project access via `getProjectWithAccess`, ensures room exists with `getOrCreateRoom`, issues access-token session with `prepareSession` granting `FULL_ACCESS` for the room with user name/avatar/color in `userInfo`, returns 403 for unauthorized. `npm run build` passes.
- **Base Canvas (Feature 11)** — `types/canvas.ts` defines `NodeData` (label, color, shape), `NodeShape` (6 shapes from `NODE_SHAPES`), `NODE_COLORS` (8 dark fill + text pairs from ui-context), `CanvasNode` (`Node<NodeData, "canvasNode">`), `CanvasEdge` (`Edge<Record<string, unknown>, "canvasEdge">`); `components/editor/canvas.tsx` — `"use client"`, `useLiveblocksFlow<CanvasNode, CanvasEdge>` with `suspense: true`, empty initial nodes/edges, `ReactFlow` with `ConnectionMode.Loose`, `fitView`, `Background` (Dots), `MiniMap`, `Cursors`; CSS imports for `@xyflow/react`, `@liveblocks/react-ui`, `@liveblocks/react-flow`; `components/editor/canvas-room-wrapper.tsx` — `LiveblocksProvider` (authEndpoint `/api/liveblocks-auth`) → `RoomProvider` (id=roomId, initialPresence cursor null/isThinking false) → class `CanvasErrorBoundary` → `ClientSideSuspense` → `Canvas`; `WorkspaceShell` canvas placeholder replaced with `<CanvasRoomWrapper roomId={project.id} />`. `npm run build` passes.
- **Shape Panel (Feature 12)** — `components/editor/canvas-node.tsx` — `CanvasNodeRenderer` custom node for type `"canvasNode"`; renders as a bordered rectangle with label centered, 4 source handles (top/right/bottom/left), fill/text colors looked up from `NODE_COLORS`; `components/editor/shape-panel.tsx` — floating pill toolbar at bottom-center of canvas, 6 draggable buttons (Square→rectangle, Diamond→diamond, Circle→circle, Pill→pill, Database→cylinder, Hexagon→hexagon) using Lucide icons; `onDragStart` sets `application/x-ghost-shape` MIME data as `{ shape, width, height }` JSON with sensible defaults (rectangle 160×80, diamond 150×150, circle 80×80, pill 160×64, cylinder 100×100, hexagon 120×120); `components/editor/canvas.tsx` restructured — `Canvas` wraps `ReactFlowProvider` → `CanvasContent`; `CanvasContent` uses `useReactFlow().screenToFlowPosition` for drop coordinate conversion, `onDragOver`/`onDrop` handlers on the wrapper div; `onDrop` parses payload, converts screen→flow position, centers node on cursor, creates `CanvasNode` with empty label + default color + dragged shape, calls `onNodesChange([{ type: "add", item }])`; node IDs follow `${shape}-${Date.now()}-${counter}` pattern; `nodeTypes` registers `canvasNode` → `CanvasNodeRenderer`. `npm run build` passes.
- **Node Shapes + Drag Preview (Feature 13)** — `canvas-node.tsx` rewritten with `ShapeFrame` component: rectangle uses CSS `border-radius: 0.75rem`; pill and circle use `border-radius: 9999px`; diamond/hexagon/cylinder render inline SVG (`preserveAspectRatio="none"`, scales with node dimensions); selected stroke switches to `var(--accent-primary)` (teal), rest state uses `var(--border-subtle)`; `shape-panel.tsx` gains `buildSvgPreview` helper and `setDragImage` call in `handleDragStart` — a temporary off-screen `<div>` matching the shape's dimensions and appearance (CSS border-radius for CSS shapes, SVG innerHTML for SVG shapes) is set as the browser drag image centered on cursor, then removed in the next animation frame; `canvas.tsx` adds `wrapperRef` + `insertShapeAtClientPoint` + `handleInsertShape` wired to `ShapePanel`'s `onInsert` prop (click-to-insert fallback centered in viewport). `npm run build` passes.
- **Node Editing (Feature 14)** — `canvas-node.tsx` gains `NodeResizer` (from `@xyflow/react`) with `isVisible={selected}`, `minWidth=60`, `minHeight=40`, and subtle teal handles (`var(--accent-primary)`, 8×8px, 0.7 opacity); inline label editing activated by double-clicking the node — a textarea replaces the label span, marked `nodrag nopan nowheel` to block canvas drag/pan/zoom, commits on blur, cancels on Escape; label updates via `useReactFlow().updateNodeData(id, { label })` which routes through `BatchProvider` → `onNodesChange` → Liveblocks storage; empty label renders a dimmed placeholder. `npm run build` passes.
- **Node Color Toolbar (Feature 15)** — `canvas-node.tsx` gains `ColorSwatch` and `ColorToolbar` components; toolbar is a floating pill (same style as ShapePanel) positioned 10px above the selected node via `absolute bottom: calc(100% + 10px)` inside a `relative overflow-visible` root div; shows 8 swatches from `NODE_COLORS`; active swatch has a 2px white ring; hovered swatch shows a tight box-shadow glow in the swatch's paired text color (parsed from hex to rgba); clicking a swatch calls `updateNodeData(id, { color: fill })` through the existing Liveblocks collaborative path; toolbar is marked `nodrag nopan nowheel` with `stopPropagation` on mousedown to prevent canvas drag/pan; node background and text color update immediately. `npm run build` passes.

- **Edge Behavior (Feature 16)** — `types/canvas.ts` adds `EdgeData` interface (`label?: string`) and updates `CanvasEdge` type; `components/editor/canvas-edge.tsx` new custom edge renderer: `getSmoothStepPath` for right-angle routing, wide invisible hit area path (`strokeWidth=16`, `strokeOpacity=0`, `pointerEvents: stroke`) for easy hover/click, `BaseEdge` for the visible stroke (dimmed at rest, bright on hover/selected, teal when selected), `EdgeLabelRenderer` + path midpoint `[labelX, labelY]` for HTML label positioned at the edge center, double-click to edit label with auto-growing controlled input, saved labels rendered as pill badges, faint dashed hint when edge is active but unlabeled, all label interactions marked `nodrag nopan nowheel` with `stopPropagation`; `canvas.tsx` registers `edgeTypes`, adds `defaultEdgeOptions` with `type: "canvasEdge"` and `MarkerType.ArrowClosed`; `globals.css` styles handles as 8×8px white dots with dark border, hidden by default (`opacity: 0`), faded in on node hover, node selection, and during active connection. `npm run build` passes.

- **Canvas Ergonomics (Feature 17)** — `hooks/useKeyboardShortcuts.ts` new hook: receives a `Zoomable` instance (`zoomIn`/`zoomOut`) and `onUndo`/`onRedo` callbacks, attaches a `keydown` listener on `window`, skips editable fields (INPUT, TEXTAREA, `isContentEditable`); shortcuts: `+`/`=` → zoom in, `-` → zoom out, `Ctrl/Cmd+Z` → undo, `Ctrl/Cmd+Shift+Z` → redo, `Ctrl/Cmd+Y` → redo; all zoom calls use `{ duration: 200 }` for smooth animation; `canvas.tsx` updated: `MiniMap` removed; `ViewportIndicator` replaced by `CanvasControlBar` — a pill at `absolute bottom-6 left-3` with zoom group (Minus → zoomOut, Maximize2 → fitView with `duration:300`, Plus → zoomIn) and history group (Undo2/Redo2) separated by a thin divider; undo/redo buttons disabled and dimmed (`opacity-40`) when `useCanUndo()`/`useCanRedo()` return false; Liveblocks `useUndo`, `useRedo`, `useCanUndo`, `useCanRedo` imported from `@liveblocks/react` and wired in `CanvasContent`. `npm run build` passes.

- **Starter Templates (Feature 18)** — `components/editor/starter-templates.ts` defines `CanvasTemplate` interface (id, name, description, nodes, edges) and `CANVAS_TEMPLATES` array with three predefined templates: Microservices (API gateway → auth/user/order services → dedicated DBs), CI/CD Pipeline (commit → build → test → artifact store → staging → production), and Event-Driven System (producers → event bus → consumers + dead-letter queue); templates use shared `CanvasNode`/`CanvasEdge` types and `NODE_COLORS`; `components/editor/starter-templates-modal.tsx` dialog with a responsive grid of template cards — each card renders an inline SVG preview (bounding-box scaled to a 280×150 viewport, edges as lines between node centers, nodes as shape-appropriate SVG primitives mirroring `canvas-node.tsx` shape logic) plus name, description, and Import button; Import calls `onImport(template)` and closes the dialog; `canvas.tsx` exports `LoadTemplateFn` type and adds `loadTemplateRef` prop to `Canvas`/`CanvasContent`; `CanvasContent` registers `loadTemplateRef.current` on mount using a stable-ref pattern (always-fresh `nodesRef`/`edgesRef` to avoid stale closures); on call, batches all existing node/edge removes + template node/edge adds into single `onNodesChange`/`onEdgesChange` calls then fits view after 100ms; `canvas-room-wrapper.tsx` accepts and forwards `loadTemplateRef`; `workspace-shell.tsx` adds `useRef<LoadTemplateFn | null>`, `isTemplatesOpen` state, a "Templates" `<Button>` in the navbar (LayoutTemplate icon), `<CanvasRoomWrapper loadTemplateRef={...}>`, and `<StarterTemplatesModal onImport={(t) => loadTemplateRef.current?.(t)}>`. `npm run build` passes.

- **Presence Avatars + Live Cursors (Feature 19)** — `liveblocks.config.ts` updated: `isThinking` renamed to `thinking` to match spec; `canvas-room-wrapper.tsx` updated `initialPresence` accordingly; `components/editor/live-cursors.tsx` — new component, uses `useOthers` from `@liveblocks/react` and `useViewport` from `@xyflow/react`, transforms flow coordinates to screen coordinates (`flowX * zoom + vpX`) and renders colored SVG arrow cursors with name badges for each other participant who has a non-null cursor; `components/editor/presence-avatars.tsx` — new component, uses `useOthers` and `useAuth` (Clerk), filters others by Clerk user ID to exclude current user and any of their other tabs, renders up to 5 collaborator avatars in an overlapping stack (profile photo or initials fallback, presence-color ring via `box-shadow`), +N overflow chip when more than 5, divider only when collaborators exist, Clerk `UserButton` for current user; `canvas.tsx` imports `useUpdateMyPresence`, adds `handleMouseMove`/`handleMouseLeave` on the wrapper div to broadcast flow-coordinate cursor position via presence, removes `<Cursors />` from `@liveblocks/react-flow`, renders `<LiveCursors />` and `<PresenceAvatars />` as absolute overlays inside the canvas wrapper; workspace navbar and editor home navbar unchanged. `npm run build` passes.

- **AI Sidebar Shell (Feature 20)** — `components/editor/ai-sidebar.tsx` new component: fixed floating panel (`right-0 top-12 bottom-0 z-40`, `w-80`) with slide-in/out via `translate-x-0`/`translate-x-full` and `transition-transform duration-200`; header with `BotMessageSquare` icon, "AI Workspace" title, "Collaborate with Ghost AI" subtitle, and close button; shadcn `Tabs` with `line` variant — "AI Architect" and "Specs" tabs, active tab underlined in AI accent color (`after:bg-ai data-active:text-ai-text`); AI Architect tab: `ScrollArea`-scrollable chat area, empty state with bot icon + description + three starter prompt chips (`bg-subtle text-ai-text rounded-full`), chat message rendering (user messages right-aligned `bg-brand-dim border-brand/50`, assistant left-aligned `bg-elevated border-border-subtle text-ai-text`), auto-resizing `Textarea` (72px min / 160px max, JS height reset on send), `Enter` submits / `Shift+Enter` newline, send button `bg-ai text-white`; Specs tab: "Generate Spec" button `bg-ai text-white`, demo spec card `bg-elevated border-border-subtle rounded-2xl` with FileText icon + title + snippet + disabled download button; `workspace-shell.tsx` updated to import `AISidebar`, remove inline aside from flex layout, render `<AISidebar isOpen={isAIPanelOpen} onClose={...} />` as a sibling. `npm run build` passes.

- **Design Agent API (Feature 22)** — `prisma/models/task-run.prisma` with `TaskRun` model (`runId` unique, `projectId`, `userId`, `createdAt`; index on `runId`, compound index on `userId`+`projectId`); migration `20260701032228_add_task_run` applied; `trigger/design-agent.ts` exports `designAgentTask` (id: `"design-agent"`, echoes payload, no AI yet); `app/api/ai/design/route.ts` — POST accepts `prompt`/`roomId`/`projectId`, triggers the design task, creates `TaskRun` record, returns `runId`; `app/api/ai/design/token/route.ts` — POST accepts `runId`, verifies ownership via `TaskRun`, issues Trigger.dev public token scoped to that run; `npm run build` passes.

- **Canvas Autosave (Feature 21)** — `@vercel/blob` installed (`v2.5.0`); `canvasJsonPath String?` field on `Project` model already existed — no migration needed; `app/api/projects/[projectId]/canvas/route.ts` — PUT receives `{ nodes, edges }` JSON, calls `put("canvas/{projectId}.json", …, { allowOverwrite: true })` on Vercel Blob and stores returned URL in `canvasJsonPath` via Prisma; GET reads `canvasJsonPath` from Prisma, fetches blob, and returns `{ canvas }` or `{ canvas: null }` if not yet saved; `hooks/use-canvas-autosave.ts` — debounces saves by 2 s, skips the first render (so initial room load does not trigger a redundant save), tracks `SaveStatus` (`idle | saving | saved | error`), notifies parent via `onStatusChange` callback ref; `canvas.tsx` updated — `CanvasContent` accepts `projectId` and `onSaveStatusChange`; one-shot mount effect checks if the Liveblocks room is empty and, if so, fetches saved canvas from the GET route and batch-adds nodes/edges via `onNodesChange`/`onEdgesChange` then fits view (room with existing content skips load entirely); `useCanvasAutosave` wired in `CanvasContent`; `SaveStatus` re-exported from `canvas.tsx`; `Canvas` and `CanvasRoomWrapper` props updated to thread `projectId` / `onSaveStatusChange` through; `workspace-shell.tsx` — `saveStatus` state + `handleSaveStatusChange` callback (`useCallback`), small inline indicator in navbar after project name: `CloudUpload` + "Saving…" (pulsing), `Check` + "Saved", `CloudOff` + "Save failed" (red); hidden when `idle`. `npm run build` passes.

- **Design Agent Logic (Feature 23)** — `trigger/design-agent.ts` fully implemented: calls Gemini (`gemini-2.0-flash` via `@ai-sdk/google`) with `generateObject` and a Zod schema to produce structured nodes and edges; builds `CanvasNode`/`CanvasEdge` values using project color palette and allowed shapes; broadcasts `AI_THINKING_START`, `AI_STATUS`, `AI_CANVAS_BATCH` (canvas data serialised to JSON string), and `AI_THINKING_END` events through the Liveblocks room via `getLiveblocks().broadcastEvent`; `liveblocks.config.ts` `RoomEvent` type updated to declare all four event shapes; `canvas.tsx` uses `useEventListener` to receive `AI_CANVAS_BATCH` and apply nodes/edges through the existing `onNodesChange`/`onEdgesChange` Liveblocks collaborative flow, shows a floating AI status overlay (`AI_STATUS` text + pulsing dot) visible to all room participants; `ai-sidebar.tsx` now accepts `projectId`, calls `/api/ai/design` on send, fetches a run token from `/api/ai/design/token`, mounts a `RunSubscriber` component that uses `useRealtimeRun` from `@trigger.dev/react-hooks` to track completion, and renders a spinner status message while the task runs; `workspace-shell.tsx` passes `project.id` as `projectId` to `AISidebar`; type bridge added for `@ai-sdk/google@4.x` (`LanguageModelV4`) vs `ai@6.x` (`LanguageModelV2/V3`) incompatibility. `npm run build` passes.

- **AI Presence State (Feature 24)** — `types/tasks.ts` new file with `aiStatusFeedPayloadSchema` (Zod: `{ text?: string }`) and `AiStatusFeedPayload` type; `liveblocks.config.ts` `RoomEvent` union extended with `AI_FEED` (`{ type: "AI_FEED"; text?: string }`); `canvas-room-wrapper.tsx` refactored to export `WorkspaceRoomProvider` (wraps `LiveblocksProvider` + `RoomProvider` with `children` — covers whole workspace) and `CanvasRoom` (error boundary + `ClientSideSuspense` + `Canvas`); `workspace-shell.tsx` uses `WorkspaceRoomProvider` to wrap both the canvas area and `AISidebar` so the sidebar is inside the Liveblocks room context; `ai-sidebar.tsx` now imports `useEventListener`/`useOthers`/`useUpdateMyPresence` from `@liveblocks/react`, subscribes to the `ai-status-feed` via `AI_STATUS`/`AI_FEED` room events (validates each message through `aiStatusFeedPayloadSchema.safeParse`), tracks room-wide `isRoomAiActive` from `AI_THINKING_START`/`AI_THINKING_END` events, derives `anyThinking` (`isLoading || isRoomAiActive || others.some(o => o.presence.thinking)`), disables chat textarea and send button when `anyThinking`, shows send-button spinner when `anyThinking`, shows feed message pill in header when AI is active, sets `thinking: true` in own presence on send and clears it on run completion or error; `live-cursors.tsx` reads `other.presence.thinking` and renders a small spinning border circle inside the name badge when `true`. `npm run build` passes.

- **Sidebar Chat Feed (Feature 25)** — `types/tasks.ts` extended with `chatMessageSchema` (Zod: `{ sender, role:"user", content, timestamp }`) and `ChatMessageData`; `liveblocks.config.ts` adds `FeedMessageData` to the global `Liveblocks` interface to type the `ai-chat` feed messages; `components/editor/ai-sidebar.tsx` imports `useSelf`, `useFeedMessages`, `useCreateFeedMessage`, `useCreateFeed` from `@liveblocks/react`; sidebar gains a "Chat" tab (tabs resized to `grid-cols-3` with shortened labels "Architect"/"Chat"/"Specs"); the Chat tab creates the `ai-chat` feed on mount (idempotent), subscribes via `useFeedMessages("ai-chat")`, validates each message with `chatMessageSchema.safeParse` before rendering (invalid messages skipped), shows sender/time/content with own messages right-aligned (brand-dim bubble) and others left-aligned (elevated bubble), renders loading/error/empty states; `useCreateFeedMessage()` sends new messages with `{ sender, role:"user", content, timestamp }`, clears input on success, shows inline error on failure; `ai-chat` feed is entirely separate from `ai-status-feed` room events. `npm run build` passes.

- **AI Chat Functional (Feature 26)** — `types/tasks.ts` `chatMessageSchema.role` extended from `z.enum(["user"])` to `z.enum(["user", "assistant"])` so AI responses can be persisted to the same feed as user messages; `components/editor/ai-sidebar.tsx` Architect tab rewired to read/write through the shared `ai-chat` Liveblocks feed instead of local-only component state — removed the old `ChatMessage`/`messages`/`addMessage` local state entirely; new `pushFeedMessage(role, content)` helper calls `useCreateFeedMessage()` with `sender` set to the current user's name (`useSelf()`) for `"user"` or a constant `AI_SENDER_NAME = "Ghost AI"` for `"assistant"`, swallowing feed-push failures so they never block the design run; `handleSend` now pushes the user's prompt to `ai-chat` before calling `POST /api/ai/design` (unchanged backend contract: `{ prompt, roomId, projectId }` → `{ runId }`, followed by the existing `POST /api/ai/design/token` call for the public token), and pushes an `"assistant"` error message to the feed on any failure path instead of updating local state; `RunSubscriber`'s `onMessage` callback (`handleRunMessage`) now pushes the completion/failure message to `ai-chat` as an `"assistant"` message rather than mutating a local message list; Architect tab's message list now renders `useFeedMessages("ai-chat")` (the same feed/hook call already used by the Chat tab) with loading/error/empty states matching the Chat tab, parses each entry with `chatMessageSchema.safeParse`, and styles `role:"user"` bubbles with the design system's green `success` token (`border-success/50 bg-success/15`, no new colors introduced) and `role:"assistant"` bubbles with the existing dark `bg-elevated`/`text-ai-text` treatment; submit button now uses `bg-success` (green) instead of `bg-ai` (purple) to match the spec's "green accent" submit button while running/disabled states are unchanged (spinner + `opacity-40`); the compact status strip (`ai-status-feed` via `AI_STATUS`/`AI_FEED` room events, shown only while `anyThinking`) and canvas realtime updates via `useLiveblocksFlow` were already correct from Features 23/24 and were left untouched, per the feature's scope limits (no backend/Trigger.dev changes). The Chat tab (general room chat, Feature 25) is unchanged — since it renders the same `ai-chat` feed, AI-authored messages now also appear there automatically as "not-me" bubbles. `npm run build` passes.

- **Spec Generation Flow (Feature 27)** — `trigger/generate-spec.ts` new file exporting `generateSpecTask` as a `schemaTask` (id: `"generate-spec"`, `maxDuration: 300`); Zod schema (`generateSpecPayloadSchema`, exported along with its `z.input` type `GenerateSpecPayload`) validates `projectId`, `roomId`, `chatHistory` (reuses `chatMessageSchema` from `types/tasks.ts`), and loosely-typed `nodes`/`edges` (id/position/data or id/source/target/data, `.passthrough()` so it accepts the full `CanvasNode`/`CanvasEdge` shape without redefining `types/canvas.ts`); task summarizes nodes/edges/chat into plain-text context blocks, calls Gemini (`gemini-2.5-flash` via `@ai-sdk/google`, matching `design-agent.ts`) with `generateText` (plain Markdown output, not `generateObject`) and a system prompt describing spec structure (Overview/Components/Data Flow/Key Considerations); `metadata.set("status", …)` marks `"analyzing"` → `"generating"` → `"completed"`/`"failed"` for realtime tracking via `useRealtimeRun`/`useRun` (no Liveblocks broadcast — this task doesn't mutate canvas or chat, only returns `{ spec: string }` as task output, per scope limit "do not store the final spec in this unit"); `app/api/ai/spec/route.ts` — POST accepts `{ roomId, chatHistory, nodes, edges }` (no client-supplied `projectId`), authenticates via Clerk `auth()`, resolves the project via `getProjectWithAccess(roomId)` (same pattern as `app/(workspace)/editor/[roomId]/page.tsx` — `roomId` is the project id), 403s if access fails, triggers `generate-spec` with the resolved `project.id`, creates a `TaskRun` record, returns `{ runId }`; `app/api/ai/spec/token/route.ts` — mirrors `app/api/ai/design/token/route.ts` (verify `TaskRun.userId` ownership, `auth.createPublicToken`) but explicitly sets `expirationTime: "1h"` per spec (the design route relies on the SDK's 15-minute default). `npm run build` passes.

## In Progress

- None.

## Next Up

- Continue with next feature spec after Feature 27.

## Recent Fixes

- **Smooth project switching** — `project-sidebar.tsx`: `ProjectItem`'s `onClick` now only calls `onOpen` (close sidebar) on mobile (`< 640px`); on desktop the sidebar stays open throughout navigation. `canvas-room-wrapper.tsx`: replaced the "Connecting…" `ClientSideSuspense` fallback with a silent `bg-base` div so the Liveblocks room handshake is visually invisible. `npx tsc --noEmit` passes.

- **Edge endpoint reconnection** — Added `onReconnect`, `onReconnectStart`, `onReconnectEnd`, and `reconnectRadius={20}` to `<ReactFlow>` in `canvas.tsx`. `handleReconnect` atomically removes the old edge and re-adds an updated copy (same id, preserved label/labelOffset, updated source/target/handles) via a single `onEdgesChange` call so Liveblocks applies both changes together. A `reconnectSuccessful` ref tracks whether the drop landed on a valid handle; `handleReconnectEnd` deletes the edge if it did not (dropped on empty canvas). `Connection` type imported from `@xyflow/react`. `npx tsc --noEmit` passes.

- **Liveblocks config** — `liveblocks.config.ts` corrected: `Presence` now declares `cursor: {x,y}|null` and `isThinking: boolean`; `UserMeta.info` now declares `name`, `avatar`, `color`.
- **Drag-and-drop (round 2)** — `onDragOver` and `onDrop` moved from the wrapper `<div>` to `<ReactFlow>` props directly; this prevents React Flow's internal event handling from consuming/blocking external drag events. `onDrop` now calls `onNodesChange([{type:"add", item:newNode}])` (the library's standard path via `applyNodeChanges`→`toLiveblocksInternalNode`), replacing the previous `useMutation`+`new LiveObject` approach which produced nodes incompatible with `setLocal` (causing drag-to-move failures). `useMutation` and `LiveObject` imports removed.
- **MiniMap dark theme** — `globals.css` adds `.react-flow__minimap-svg { background: #111114 }` and `.react-flow__minimap-mask { fill: rgba(0,0,0,0.6) }` to eliminate the white box; MiniMap `style` prop sets `background: "#111114"` and border.
- **Viewport position indicator** — `ViewportIndicator` component added to `canvas.tsx` using `useViewport()`; renders as an absolute pill at `bottom-6 left-3` showing zoom% and canvas x,y offset. Lives outside `<ReactFlow>` so it doesn't interfere with React Flow's panel system.
- **Canvas visual overhaul (round 2)** — `globals.css` expanded React Flow overrides: `--xy-background-color-default` set to `transparent`, `!important` on background/box-shadow/border/outline for `.react-flow`, `.react-flow__renderer`, `.react-flow__container` to eliminate card/floating effect; `canvas.tsx` wrapper div gets explicit `bg-base` so the dark base color is always the canvas floor; `<Background>` updated to `gap={24} size={1.5} color="#4a4a5a"` for clearly visible dots on the dark background.
- **Drag-and-drop (round 3)** — Root cause identified and fixed: `onDragOver` was conditionally calling `e.preventDefault()` only when `e.dataTransfer.types.includes(SHAPE_DRAG_TYPE)` passed. If that check fails at runtime (e.g., `DOMStringList.includes` throws vs returning false in certain browser contexts, or the type isn't yet in the list), `preventDefault()` is never called and the browser silently suppresses the `drop` event entirely. Fix: `onDragOver` now unconditionally calls `e.preventDefault()` and sets `dropEffect="copy"`; `onDrop` already guards against non-shape drops via `if (!raw) return`.
- **Drag-and-drop (round 4)** — Replaced React synthetic `onDragOver`/`onDrop` props on `<ReactFlow>` with native `addEventListener("dragover")` / `addEventListener("drop")` on a `ref`'d wrapper div. React's synthetic event delegation fires after the browser has already decided whether to accept the drop, so `e.preventDefault()` on a synthetic `dragover` may not reliably unlock the drop target — native listeners execute at the DOM event level and guarantee it works. The `useEffect` re-attaches handlers when `screenToFlowPosition` or `onNodesChange` change. Also: `ShapePanel` icons get `pointer-events-none` to ensure the drag always starts from the button (not the SVG child), and `handleDragStart` now sets `text/plain` as a fallback MIME type alongside `application/x-ghost-shape`; the drop handler reads either type.
- **Drag-and-drop (round 5)** — Wrapper-level native drag/drop listeners now run in the capture phase, before React Flow pane handlers can consume the drop event. The drop path still writes through Liveblocks React Flow's `onNodesChange([{ type: "add", item }])`, and payload parsing now validates shape, width, and height against `NODE_SHAPES` before creating a canvas node. `npm run build` passes.
- **Editor reopen + canvas drop fix** — `/editor` now opens with the project sidebar visible by default; project rows in `ProjectSidebar` are real links to `/editor/{projectId}` so existing projects can be reopened after signing back in; shape drag payloads now include a visible label; canvas drops now use React Flow's `onDragOver`/`onDrop` with `useReactFlow().addNodes()` and explicit node `style` dimensions so Liveblocks React Flow receives the add through the normal controlled-flow path. `npm.cmd run build` passes.
- **Liveblocks storage key fix** — Canvas storage now uses app-owned key `ghostCanvasFlowV1` instead of Liveblocks React Flow's generic default `flow` key. Existing rooms could contain stale/malformed `flow` data from earlier experiments, which allowed initial rendering but crashed on first mutation with `flow.get is not a function`. Clearing stale `.next/dev` generated types was also required after a corrupted validator file blocked one build; fresh `npm.cmd run build` passes.
- **Liveblocks storage repair gate** — Added `CanvasStorageGate` before `useLiveblocksFlow` mounts. It checks `ghostCanvasFlowV1` and replaces malformed room storage with the LiveObject/LiveMap `{ nodes, edges }` structure required by `@liveblocks/react-flow`, preventing the repeated `flow.get is not a function` crash in existing rooms even after a malformed app-owned key already exists. `npm.cmd run build` passes.
- **Liveblocks duplicate core fix** — `CanvasStorageGate` now imports `LiveObject`/`LiveMap` from `@liveblocks/client` instead of direct `@liveblocks/core`. Direct core imports resolved to the root `@liveblocks/core@3.19.1` installed by `@liveblocks/node@3.19.1`, while browser Liveblocks packages use `@liveblocks/core@3.19.2`, causing the runtime duplicate-Liveblocks error. `npm.cmd run build` passes.
- **Liveblocks auth fetch fix** — `/api/liveblocks-auth` is now excluded from Clerk middleware redirects and enforces Clerk auth inside the route handler, returning JSON `401/403/400` errors instead of sign-in HTML. `CanvasRoomWrapper` now uses an explicit Liveblocks auth callback with `credentials: "include"` so Clerk cookies are sent reliably during room authorization.
- **React Flow drop alignment** — Shape drag payloads now use the reference MIME type `application/ghost-shape` with fallbacks for older payloads, and canvas drops now add nodes through `onNodesChange([{ type: "add", item }])` with direct `width`/`height` fields. This matches the working Liveblocks React Flow reference pattern instead of using React Flow instance `addNodes()` plus `style` dimensions.
- **Liveblocks Flow initializer fix** — Removed the custom `CanvasStorageGate` that manually created Liveblocks CRDT storage before `useLiveblocksFlow` mounted. Canvas now uses fresh storage key `ghostCanvasFlowV2` and lets `@liveblocks/react-flow` initialize the `nodes`/`edges` LiveMaps itself, avoiding stale malformed storage and duplicate-CRDT object risks from earlier repair attempts.
- **Liveblocks package dedupe fix** — Upgraded `@liveblocks/node` from `3.19.1` to `3.19.2` so all Liveblocks packages resolve to a single `@liveblocks/core@3.19.2` copy. This fixes the runtime `flow.get is not a function` crash caused by CRDT objects being created/consumed across mismatched Liveblocks core versions.
- **Liveblocks poisoned storage bypass** — Renamed the active React Flow storage key to stable `ghostCanvasFlow` after package dedupe. Earlier `ghostCanvasFlowV1`/`V2` keys were temporary bypass keys written while the duplicate-core runtime was active, so they are intentionally no longer used.
- **Code review refinements** — `getLiveblocks()` now caches its client in all environments and validates `LIVEBLOCKS_SECRET_KEY`; canvas nodes now render distinct stored shapes with stable handle ids; `CanvasErrorBoundary` resets when `roomId` changes; shape buttons now support click/keyboard insertion at the viewport center through the same node-creation path as drag/drop; workspace keeps the sidebar open by default while reserving desktop space for it, and the AI panel now participates in layout instead of covering the canvas.
- **Canvas styling** — `colorMode="dark"` on `ReactFlow`; `globals.css` strips React Flow default box-shadows on nodes (`!important`) and panels; `.react-flow__pane` cursor reset.
- **AI sidebar** — now `fixed right-0 top-12 bottom-0 z-40`, floating over canvas instead of pushing it.
- **Project sidebar** — `fixed left-0 top-12 z-40` with `translate-x-0`/`-translate-x-full` animation; floats over canvas, fully hidden when toggled off.
- **Canvas keyboard deletion** — Delete/Backspace now use a focusable canvas wrapper keydown listener, read selected nodes/edges from React Flow `useNodes()`/`useEdges()`, and remove them through Liveblocks React Flow `onDelete` so collaborative deletion syncs; deleting selected nodes also removes their connected edges.
- **AI status/branding cleanup (post-Feature 26)** — `canvas.tsx` no longer listens for `AI_THINKING_START`/`AI_THINKING_END`/`AI_STATUS` room events or renders its own floating status pill; it now only handles `AI_CANVAS_BATCH` (applying AI-generated nodes/edges). Status display lives exclusively in `ai-sidebar.tsx`'s header strip, which already listened to the same events independently. `trigger/design-agent.ts` broadcast copy no longer names the model provider — `"Analyzing your prompt…"` → `"Ghost AI is analyzing your request…"`, `"Generating design with Gemini…"` → `"Ghost AI is designing your architecture…"`. `workspace-shell.tsx`'s AI panel toggle is now a labeled `variant="outline"` button ("AI Chat" + Sparkles icon, matching the Templates/Share buttons) instead of an icon-only ghost button, with an active/tinted state (`bg-ai/15 text-ai-text`) when the panel is open. `npm run build` passes.
- **Liveblocks Feeds permission fix (critical)** — Root-caused why `ai-chat` feed messages (both the user's prompt and the AI's response) never appeared in the sidebar: `app/api/liveblocks-auth/route.ts` granted `session.FULL_ACCESS`, which resolves to `["room:write"]` only (confirmed in `node_modules/@liveblocks/node/dist/index.d.ts`) — Liveblocks Feeds require the separate `"feeds:write"` permission, which was never granted, so every `createFeed`/`createFeedMessage` call was silently rejected server-side and swallowed by the client's `catch`. Fixed by changing the grant to `session.allow(room, [...session.FULL_ACCESS, "feeds:write"])`. `ai-sidebar.tsx`'s `pushFeedMessage` now also `console.error`s on failure instead of swallowing silently, to make any future regression visible in devtools. Also moved the AI status strip (from `ai-status-feed` room events) from the sidebar header down to sit directly above the Architect tab's input box, and added a green "Working…" badge next to the "AI Workspace" header title (shown whenever `anyThinking` is true) so panel activity is visible even when scrolled or on other tabs. `npm run build` passes.
- **Liveblocks Feeds creation moved server-side (final fix)** — After the permission fix, `ai-chat` messages still didn't reliably appear: the client's websocket-based `createFeed("ai-chat")` (called from `ai-sidebar.tsx` on mount) raced independently against `useFeedMessages("ai-chat")`, and on a brand-new room a lost race produced a permanent, non-self-healing `FEED_NOT_FOUND` `LiveblocksError`. A client-side fix (splitting `AISidebar` into a gating wrapper + inner panel, deduping concurrent `createFeed` calls, retrying) got the race under control but then hit `Error: Feed mutation timeout` — Liveblocks' real-time feed-mutation path has a fixed 5s client timeout (`FEEDS_TIMEOUT` in `node_modules/@liveblocks/core/dist/index.js`) that proved unreliable for a room's very first feed creation even after deduping. Replaced all of that client-side gating with a proper fix: `app/api/liveblocks-auth/route.ts` now calls the REST-based `liveblocks.createFeed({ roomId, feedId: "ai-chat" })` (from `@liveblocks/node`, a separate code path from the client SDK's websocket mutations) right after `getOrCreateRoom`, and awaits it before ever issuing the client's auth token — so the feed is guaranteed to exist before the client's websocket connection is even established, eliminating the race entirely. 409 (`LiveblocksError` with `status === 409`) is treated as the expected "already exists" case; anything else is logged. `ai-sidebar.tsx` is back to a single component with no client-side feed creation, gating, or retry logic — trusting the server-side guarantee instead. `npm run build` passes.

## Open Questions

- Add unresolved product or implementation questions here.

## Architecture Decisions

- Dark only — no light mode. All color tokens defined in `globals.css` as CSS custom properties, mapped to Tailwind via `@theme inline`. shadcn variables point to design system tokens so components inherit the dark theme automatically.
- shadcn components live in `components/ui/` and are not modified after generation.
- `--radius` base is `0.75rem`; `@theme inline` derives `radius-xl/2xl/3xl/4xl` from it to match the border-radius scale in ui-context.md.
- Route groups: `app/(auth)/` for sign-in and sign-up pages (no EditorShell); `app/(editor)/` for the editor workspace (EditorShell via group layout). Root layout provides only ClerkProvider + html/body; no page-level chrome.
- `proxy.ts` (Next.js 16 name for middleware) defines public routes from `NEXT_PUBLIC_CLERK_SIGN_IN_URL` / `NEXT_PUBLIC_CLERK_SIGN_UP_URL` env vars; all other routes are protected by `auth.protect()`.

## Session Notes

- Next.js 16.2.6 + Tailwind v4 (`@tailwindcss/postcss`, `@import "tailwindcss"` in globals.css — no tailwind.config.js).
- shadcn 4.7.0 supports Tailwind v4 natively; uses CSS variable tokens in globals.css.
- Design system Tailwind utilities: `bg-base`, `bg-surface`, `bg-elevated`, `bg-subtle`, `text-copy-primary`, `text-copy-secondary`, `text-copy-muted`, `text-copy-faint`, `bg-brand`, `text-brand`, `bg-ai`, `text-ai-text`, `bg-success`/`warning`/`error`, `border-border-subtle`.
- Clerk v7 (`@clerk/nextjs@7.3.5`) — `clerkMiddleware` exported from `@clerk/nextjs/server`; `ClerkProvider`, `SignIn`, `SignUp`, `UserButton` from `@clerk/nextjs`. `dark` theme from `@clerk/ui/themes`.
- In Next.js 16, `middleware.ts` is renamed to `proxy.ts` — same API, new filename convention.
- Prisma 7.8.0 — `prisma.config.ts` drives schema dir (`prisma/`), migrations path, and datasource URL (overrides schema file). Schema dir is recursively scanned for `.prisma` files. Client output at `app/generated/prisma/`; import via `@/app/generated/prisma/client`. `PrismaClient` in v7 requires either `adapter` or `accelerateUrl` (mutually exclusive). Accelerate URLs start with `prisma+postgres://`; direct pg uses `new PrismaPg({ connectionString })`.
- `@liveblocks/node` must be lazy-initialized (call `getLiveblocks()` at request time, not module-level) because the `Liveblocks` constructor validates the secret key format and throws if `LIVEBLOCKS_SECRET_KEY` is absent — which breaks `npm run build` during "Collecting page data".
